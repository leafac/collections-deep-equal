{
  "scripts": {
    "build": "tsc",
    "test": "prettier --check 'src/**' && tsc && jest"
  },
  "dependencies": {
    "deep-equal": "^2.0.1"
  },
  "devDependencies": {
    "@types/jest": "^25.1.3",
    "jest": "^25.1.0",
    "prettier": "^1.19.1",
    "typescript": "^3.8.3"
  },
  "jest": {
    "rootDir": "lib",
    "testEnvironment": "node"
  }
}
